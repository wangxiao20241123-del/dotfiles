# dotfiles

Mac 开发环境迁移脚本。新机器上打开终端：

```bash
git clone git@github.com:wangxiao20241123-del/dotfiles.git ~/dotfiles
cd ~/dotfiles
./bootstrap.sh
```

## 当前包含

- GitHub 环境（git + gh + SSH 密钥 + .gitconfig）
